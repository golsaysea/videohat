# Same-origin API if the frontend and Worker are served together.
# For a separate Worker, use for example: https://videohat-api.your-subdomain.workers.dev
VITE_VIDEOHAT_API_BASE=

# Admin publishing uses a Worker secret, not a frontend env var:
# npx wrangler secret put ADMIN_TOKEN